type BizItem {
  businessId: String
  bizItemId: String
  slotMapId: [String]
}

type Query {
  bizItemInfo(bizItemId: String!): JSON!
  bizItems: [BizItem]!
  bizItem(bizItemId: String!): BizItem!
}

type Mutation {
  createBizItem(bizItemId: String!, businessId: String!): BizItem!
}
